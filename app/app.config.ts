export default defineAppConfig({
  ui: {
    colors: {
      primary: "brand",
      secondary: "gold",
      neutral: "linen",
    },
    button: {
      slots: {
        base: "cursor-pointer",
      },
    },
  },
});
